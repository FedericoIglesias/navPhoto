import { createSlice } from "@reduxjs/toolkit";
import React from "react";

let initialState = []

let  favoriteSlice = createSlice({
    name: 'favorite',
    initialState,
    reducers: {},
    
})



